# aiSelfTest 服务端功能需求


## 1. 系统定位

`aiSelfTest` 是一个面向“上游影像识别结果二次复核”的服务端系统。

系统需要连接第三方客户端平台，查询文件及识别记录，对识别框区域进行裁剪，
调用多模态模型重新判断物种，生成复核结果，并在确认后把结果回写到上游平台。

系统应同时承担以下职责：

- 管理上游客户端连接配置与认证状态。
- 管理任务定义、筛选条件、执行进度与执行结果。
- 对图片或视频的识别框做 AI 二次复核。
- 管理复核结果、确认状态和回写状态。
- 管理多模态模型配置与对话测试。
- 对外提供仪表盘统计与前端依赖接口。

## 2. 核心参与方

- **系统操作员**：配置客户端、创建任务、发起执行、查看复核结果、确认回写。
- **第三方客户端平台**：提供认证、文件分页查询、文件详情查询、AI 结果回写。
- **多模态模型网关**：提供模型列表探测、聊天补全、多模态识别能力。
- **本地服务端**：负责任务编排、媒体处理、AI 调用、结果持久化与状态维护。
- **SQLite 数据库**：保存客户端、任务、复核记录、多模态模型配置。

## 3. 核心数据模型

### 3.1 Client

用途：保存上游客户端平台连接配置与认证缓存。


功能需求：

- 系统应支持维护多个客户端配置。
- 系统应持久化访问令牌和刷新令牌，避免每次请求都重新登录。
- 系统对外返回客户端信息时，应对密码和令牌做脱敏处理。
- 系统更新客户端时，应支持“保留原密码”的提交方式。

### 3.2 Config
用途：模型提示词配置 保存用于给大模型的提示词。


功能需求：

- 用于任务使用的提示词以及格式解析

### 3.3 Task

用途：保存任务定义、筛选条件、执行配置与执行进度。


功能需求：

- 一个任务应绑定一个客户端。
- 任务应保存默认筛选条件。
- 任务应记录执行状态、处理总量、处理进度、开始结束时间和错误信息。
- 任务应具备启停控制能力。

### 3.4 TaskItem

用途：保存单个文件级信息,状态。


功能需求：

- 创建任务后每个文件流程 文件下载->大模型识别->确认(自动/手动)-> 不一致提交远端->提交训练->结束
                                                    -> 一致->结束
- 一个 TaskItem 对应一个文件结果。
- TaskItem 应区分"文件下载状态","大模型识别状态",“本地复核状态”,“远端回写状态”和"提交训练状态"。

### 3.5 TaskItemData

用途：保存单个文件级的识别信息。


### 3.6 MultimodalModel

用途：保存多模态模型网关配置。


功能需求：

- 系统应支持维护多个多模态模型配置。
- 系统应缓存探测到的模型名列表。
- API Key 对外返回时应脱敏。

## 4. 启动与通用运行能力

### 4.1 启动要求

服务启动时应按以下顺序完成初始化：

1. 配置数据库引擎。
2. 执行数据库迁移。
3. 恢复中断任务状态。

### 4.2 中断任务恢复

如果数据库中存在运行中的任务，但当前进程没有对应执行线程，系统应在启动时
将这些任务收口为失败状态，并写入结束时间和中断原因。

### 4.3 HTTP 通用要求

- 系统应启用 CORS。
- 每个请求应生成或透传 `X-Request-ID`。
- 系统应限制请求体大小，默认上限为 50MB。
- 超限请求应返回 `413`。
- 系统应记录请求日志、异常日志和关键外部调用日志。

### 4.4 静态资源托管

- 非 `/api` 路径应由静态资源服务处理。
- 系统应阻止目录穿越访问。
- `index.html` 应避免强缓存。
- 带哈希的静态资源应允许长缓存。
- 静态资源不存在时应返回 `503`。

## 5. 客户端配置与上游认证需求

### 5.1 客户端管理

系统应支持：

- 查询全部客户端配置。
- 创建客户端配置。
- 更新客户端配置。
- 删除客户端配置。

### 5.2 客户端返回结构

客户端列表和详情返回时应包含：


### 5.3 上游认证流程

系统应具备以下认证能力：

- 登录：`POST /auth/login`
- 刷新：`POST /auth/refresh`
- 使用访问令牌调用业务接口

认证策略应为：

1. 优先复用未临近过期的访问令牌。
2. 无可用访问令牌时优先尝试刷新。
3. 刷新失败时执行完整登录。

### 5.4 上游请求兼容

系统发送上游请求时应：

- 支持直接使用访问令牌作为 `Authorization`。
- 在检测到 token 失效时自动重新登录并重试。
- 在需要时回退为 `Bearer <token>` 形式。

判定 token 异常时，至少应兼容：

- HTTP `401`
- 返回消息中含有 `token`

### 5.5 删除联动

删除客户端时，系统应同步处理其关联任务和任务下的数据，避免外键冲突或残留脏数据。

## 6. 任务管理需求

### 6.1 基础能力

系统应支持：

- 获取任务列表。
- 获取单个任务详情。
- 创建任务。
- 删除任务。
- 更新任务启停状态。

### 6.2 创建任务

创建 的时候需要把筛选条件保存,后面都使用这个筛选条件,不能更改

### 6.3 任务删除

删除任务时，系统应同步删除该任务关联的所有数据。

### 6.4 任务启停

任务状态控制应至少支持：

- 启动或标记为启用
- 停止或标记为暂停
- 与运行时停止信号协同工作

## 7. 任务筛选与查询需求

### 7.1 查询入口

系统应支持：

- `POST /api/tasks/{task_id}/query-data`

用于按任务筛选条件查询上游文件数据。

### 7.2 支持的筛选字段

当前需求范围内应支持：

- `classifyList`
- `keyword`
- `spName`
- `startTime`
- `endTime`
- `fileBmp`
- `uploadType`
- `idType`
- `size`
- `current`

### 7.3 筛选清洗规则

- 日期筛选应支持规整为标准日期或日期时间边界。
- `startTime` 应按当天起始时刻处理。
- `endTime` 应按当天结束时刻处理。
- `fileBmp` 应支持 `image`、`video`、`audio`、`all`。
- 应兼容历史筛选值编码。
- `size` 和 `current` 应保证为正整数。

### 7.4 查询构造规则

查询时，系统应：

1. 读取任务保存的筛选条件。
2. 用本次请求覆盖默认筛选条件。
3. 构造上游查询请求体。

### 7.5 上游查询接口

系统应调用：

- `POST /openApi/icFile/findFilePage`


## 8. 任务执行需求

### 8.1 执行入口

系统应支持两个执行入口：

- `POST /api/tasks/{task_id}/run-now`

### 8.2 run-now 需求

`run-now` 应表示“按当前筛选条件先查后跑”，流程为：

1. 读取任务。
2. 校验任务当前不在运行中。
3. 查询符合条件的上游文件。
4. 将查询结果转成执行项。
5. 在后台启动执行。


### 8.3 并发约束

- 同一个任务同一时刻只允许一个后台执行实例。
- 运行中的任务再次启动时应返回冲突语义错误。
- 已完成的旧执行句柄应可安全清理后再重启。

### 8.4 任务状态流转

任务执行过程中应具备以下状态语义：

- 开始执行时进入 `运行`
- 收到停止信号时进入 `暂停`
- 全部处理完成时进入 `已完成`
- 批处理外围异常时进入 `失败`


## 9. 单文件处理需求

### 9.1 文件处理主流程

系统对每个待执行文件应执行以下流程：

1. 调用上游详情接口获取完整文件详情。
2. 合并列表页和详情页数据。
3. 读取 `recordData`。
4. 为每条记录生成复核行。
5. 汇总成文件级 TaskItem。
6. 将 TaskItem 落库并更新任务进度。

### 9.2 详情接口依赖

系统应依赖：

- `GET /openApi/icFile/getResultByFileId1?fileId=...`

如果详情接口不存在或失败，系统应把问题转成可追踪的错误复核结果，而不是直
接中断整批任务。

### 9.3 单文件失败容错

系统应遵循“局部失败不拖垮整批任务”的原则：

- 单文件失败时，应生成错误 TaskItem 继续处理后续文件。
- 单记录失败时，应生成错误复核行继续处理同文件其他记录。
- 只有批处理外围异常才应导致任务整体失败。

## 10. TaskItem 查询与管理需求

### 10.1 列表查询

系统应支持：

- 按 `status` 查询 TaskItem
- 按 `taskId` 过滤 TaskItem
- 按时间倒序返回

### 10.2 删除能力

系统应支持：

- 删除单条 TaskItem
- 批量删除 TaskItem

批量删除时，对不存在的记录应忽略并继续。

## 11. TaskItem 确认与回写需求

### 11.1 确认入口

系统应支持：

- `POST /api/taskItem/confirm`


### 11.2 上游回写接口

系统应调用：

- `POST /openApi/icFile/aiPollingResult`

请求体中应包含：

- 文件 ID
- 最终提交的 `recordData`

系统应允许提交空数组，表示“确认该文件无可提交记录”。

## 12. 多模态模型管理需求

系统应支持：

- 查询模型配置列表
- 探测远端可用模型
- 创建模型配置
- 更新模型配置
- 删除模型配置
- 对指定模型配置做聊天测试

### 12.1 探测入参

探测模型时必须提供：

- `endpointUrl`
- `apiKey`

### 12.2 探测返回

探测成功后应返回：

- `models`
- `detectedUrl`
- `recommendedModel`

### 12.3 模型配置更新规则

- `modelName`
- `endpointUrl`
- `apiKey`

均应视为核心字段。

当更新请求提交空值或掩码占位时，系统应支持保留已有 API Key。

## 13. 模型网关兼容需求

### 13.1 候选端点

系统应根据配置的 `endpointUrl` 自动尝试不同候选端点，至少包括：

- `/v1/models`
- `/models`
- `/v1/chat/completions`
- `/chat/completions`

### 13.2 认证头兼容

模型网关调用时应兼容多种认证头形式，例如：

- `Authorization: Bearer <api_key>`
- `X-API-Key: <api_key>`
- `api-key: <api_key>`

### 13.3 响应结构兼容

系统应从多种响应结构中解析出模型文本结果，至少兼容：

- `choices[0].message.content`
- `output_text`
- `output[].content`

## 14. 多模态聊天需求

系统应把前端消息归一化为兼容 `chat/completions` 的结构。

规则应包括：

- 仅接受 `system`、`user`、`assistant`
- 图片附件映射为 `image_url`
- 音频 data URL 映射为 `input_audio`
- 文本附件转成截断文本片段
- 非结构化附件转成说明性文本

聊天接口返回至少应包含：

- `reply`
- `modelName`
- `usedUrl`


## 15. 仪表盘需求

系统应提供：

- `GET /api/dashboard/stats`

返回以下统计项：

- `activeTasks`
- `processedToday`
- `pendingReviews`
- `anomalies`
- `recentActivities`

统计口径至少包括：

- 运行中任务数
- 当天处理的 Review 数
- 待确认 Review 数
- 执行失败任务数
- 最近完成任务摘要

## 16. 非功能需求

### 16.1 存储

- 使用 SQLite。
- 启用 WAL。
- 启用 `busy_timeout`。
- 启用外键约束。

### 16.2 性能与运行

- 客户端接口请求应具备超时控制。
- 媒体下载应具备超时控制。
- 阻塞型外部 I/O 应尽量放入后台线程执行，避免阻塞事件循环。

### 16.3 安全

- 敏感字段对外返回时必须脱敏。
- 静态资源访问必须防目录穿越。
- 请求体大小必须受限。

### 16.4 日志与可追踪性

- 记录请求级日志。
- 记录任务生命周期日志。
- 记录上游客户端调用日志。
- 记录模型网关调用日志。
- 关键错误必须保留可读错误信息。
