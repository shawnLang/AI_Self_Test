# 多模态模型管理与模型测试功能 Test.md

## 1. 本轮目标

本轮实现范围聚焦于“多模态模型管理与模型测试 V2”，覆盖以下能力：

- 后端多模态模型配置的增删改查接口
- 多模态模型配置列表与详情字段的统一响应封装
- API Key 脱敏返回
- 编辑模型配置时支持“保留原 API Key”
- 远端模型列表自动探测与缓存
- 模型网关候选端点与认证头兼容
- 多模态聊天测试的**非流式接口**
- 多模态聊天测试的**流式接口**
- 模型测试会话与消息内容持久化入库
- 基于会话历史的上下文续聊
- 前端模型管理页与模型测试页和后端接口契约对齐

本轮暂不实现：

- 任务执行链路中真实接入多模态模型配置
- 会话级权限隔离与用户归属
- 流式中断后的断点续传
- 音视频内容的服务端转码、裁剪与异步预处理

## 2. 验收标准

### 2.1 后端注册与启动

- 应用入口可成功导入并创建 FastAPI `app`
- `/api/multimodal-models` 相关路由已注册
- 保持统一响应格式 `{code, message, data}`
- 流式聊天接口返回 `text/event-stream`

### 2.2 模型配置列表

- 可返回模型配置列表
- 返回字段至少包含：
  - `id`
  - `modelName`
  - `endpointUrl`
  - `apiKey`
  - `apiKeyConfigured`
  - `status`
  - `detectedModels`
  - `lastDetectedAt`
- `apiKey` 必须脱敏
- 禁用状态与启用状态需明确区分

### 2.3 新建模型配置

- 提交合法参数可创建成功
- `modelName`、`endpointUrl`、`apiKey` 为必填
- `endpointUrl` 必须是合法 HTTP/HTTPS 地址
- 创建成功后可正常回读

### 2.4 编辑模型配置

- 可修改 `modelName`、`endpointUrl`、`status`
- 当 `apiKey` 为空字符串或掩码占位时，数据库保留原 API Key
- 当 `apiKey` 为新值时，数据库更新为新 API Key
- 更新后缓存模型列表可随请求一起保存

### 2.5 删除模型配置

- 删除存在的模型配置返回成功
- 删除不存在的模型配置返回资源不存在错误

### 2.6 模型探测

- 可通过 `endpointUrl` 和 `apiKey` 发起探测
- 探测成功返回：
  - `models`
  - `detectedUrl`
  - `recommendedModel`
- 可在多个候选端点间自动回退
- 可兼容多种认证头形式
- 探测结果可写入模型配置缓存字段

### 2.7 模型聊天测试

- 可对指定模型配置发起聊天测试
- 同时支持**非流式访问**与**流式访问**
- 仅接受 `system`、`user`、`assistant` 三种角色
- 图片附件可映射为 `image_url`
- 音频 Data URL 可映射为 `input_audio`
- 文本附件可转换为截断文本片段
- 非结构化附件可转换为说明性文本
- 非流式返回字段至少包含：
  - `reply`
  - `modelName`
  - `usedUrl`
  - `sessionId`
- 流式返回至少包含：
  - 会话元信息事件
  - 文本增量事件
  - 结束事件

### 2.8 会话与消息持久化

- 第一次发起模型测试时可自动创建会话
- 后续请求可基于 `sessionId` 延续上下文
- 每次成功完成的一轮问答，用户消息和模型回复都必须入库
- 可查询会话列表
- 可查询会话详情与完整消息列表
- 可删除指定历史会话
- 删除会话时会同时删除其下全部消息记录
- 会话详情中的消息顺序必须稳定可重建

### 2.9 模型网关兼容

- 认证头至少兼容：
  - `Authorization: Bearer <api_key>`
  - `X-API-Key: <api_key>`
  - `api-key: <api_key>`
- 非流式文本结果解析至少兼容：
  - `choices[0].message.content`
  - `output_text`
  - `output[].content`
- 流式文本结果解析至少兼容 OpenAI 风格 chunk：
  - `choices[0].delta.content`
  - `choices[0].text`

### 2.10 前端页面

- 模型管理页能正常加载配置列表
- 模型管理页能新增、编辑、删除模型配置
- 模型管理页能执行模型探测并显示返回信息
- 模型测试页能加载启用中的模型列表
- 模型测试页能查看当前模型下的历史会话
- 模型测试页能切换流式 / 非流式模式
- 模型测试页能继续历史会话并发送上下文
- 模型测试页能发送文本与附件并显示模型回复

## 3. 验证方式

### 3.1 自动验证

- Python 侧：
  - 模型配置接口单元/集成测试
  - API Key 保留与脱敏测试
  - 模型探测成功/失败测试
  - 候选端点与认证头兼容测试
  - 聊天请求归一化测试
  - 非流式聊天响应结构兼容解析测试
  - 会话创建、消息入库与历史查询测试
  - 会话删除测试
  - 流式聊天事件输出测试
- 前端侧：
  - TypeScript 类型检查

### 3.2 手工验证

1. 启动后端与前端开发服务
2. 打开模型管理页面
3. 新增一个模型配置
4. 执行模型探测，确认候选模型列表显示正常
5. 编辑该模型配置且不修改 API Key，确认原值仍保留
6. 再次编辑该模型配置并输入新 API Key，确认已更新
7. 进入模型测试页面
8. 新建会话后以**非流式**方式发送文本，确认回复成功且会话落库
9. 继续同一会话再发一轮，确认上下文有效
10. 切换到**流式**方式发送文本，确认页面增量显示回复
11. 刷新页面后重新进入该会话，确认历史消息可回显
12. 删除某个历史会话，确认列表中消失且详情接口不可再访问

## 4. 风险点

- 当前仓库没有现成的聊天会话表，需要新增模型与迁移
- 流式网关实现依赖上游兼容 OpenAI 风格 SSE chunk
- 附件以 JSON 入库会增加 SQLite 文件体积
- 若不同模型网关的流式 chunk 结构差异较大，后续仍需扩展兼容分支
