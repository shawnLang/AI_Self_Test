# 客户端管理后台功能 Test.md

## 1. 本轮目标

本轮实现范围聚焦于“客户端管理后台 V1”，覆盖以下能力：

- 后端应用入口修复到可启动状态
- 客户端配置的增删改查接口
- 客户端认证能力：token 复用、刷新、登录
- 客户端敏感字段脱敏返回
- 编辑客户端时支持“保留原密码”
- 删除客户端时联动删除关联任务及任务数据
- 前端客户端管理页面与后端接口契约对齐
- 任务创建页读取客户端列表时可正常工作

本轮暂不实现：

- 上游平台真实登录、刷新 token、自动重试
- 任务执行线程、定时调度、媒体下载、模型识别
- Review、模型管理、仪表盘完整业务逻辑

## 2. 验收标准

### 2.1 后端启动

- 应用入口可成功导入并创建 FastAPI `app`
- `/health` 可返回成功响应
- `/api/clients` 路由已注册

### 2.2 客户端列表

- 可返回客户端列表
- 返回结构统一为 `{code, message, data}`
- `password`、`accessToken`、`refreshToken` 字段必须脱敏
- 列表字段至少包含：
  - `id`
  - `name`
  - `apiUrl`
  - `account`
  - `status`
  - `password`
  - `accessToken`
  - `refreshToken`
  - `expiresIn`

### 2.3 新建客户端

- 提交合法参数可创建成功
- 缺少必要字段时返回 `code=1001`
- 状态仅允许既定枚举值

### 2.4 编辑客户端

- 可修改 `name`、`apiUrl`、`account`、`status`
- 当 `password` 为空字符串或掩码占位时，数据库保留原密码
- 当 `password` 为新值时，数据库更新为新密码

### 2.5 删除客户端

- 删除存在的客户端返回成功
- 客户端下关联的 `Task`、`TaskItem`、`TaskItemData` 一并删除
- 删除不存在的客户端返回资源不存在错误

### 2.6 前端页面

- 客户端页面能正常加载列表
- 新建、编辑、删除操作能调用真实接口
- 编辑弹窗中密码字段支持“留空表示不修改”
- 前端状态值与后端枚举保持一致
- 页面可触发客户端认证
- 页面可展示认证状态
- 任务创建页可加载客户端列表

### 2.7 客户端认证

- 当本地存在未临近过期的 `accessToken` 时，优先复用，不发起上游登录
- 当 `accessToken` 不可用且存在 `refreshToken` 时，优先调用 `/auth/refresh`
- 当刷新失败时，自动回退调用 `/auth/login`
- 认证成功后，新的 `accessToken`、`refreshToken`、`expiresIn` 会持久化到数据库
- 系统对外返回客户端信息时，token 仍保持脱敏
- 系统可提供手动触发认证的接口，用于客户端管理页测试连接
- 业务请求封装需支持：
  - 先尝试 `Authorization: <token>`
  - 必要时回退为 `Authorization: Bearer <token>`
  - 检测到 token 失效后自动重新认证并重试

## 3. 验证方式

### 3.1 自动验证

- Python 侧：
  - 客户端接口单元/集成测试
  - 删除联动测试
  - 密码保留与脱敏测试
  - 应用入口导入测试
  - 认证复用/刷新/登录测试
  - 业务请求自动重试测试
- 前端侧：
  - TypeScript 类型检查

### 3.2 手工验证

1. 启动后端与前端开发服务
2. 打开客户端管理页面
3. 新建一个客户端
4. 编辑该客户端且不修改密码，确认原密码仍保留
5. 再次编辑该客户端并输入新密码，确认已更新
6. 删除该客户端，确认列表移除
7. 进入创建任务页面，确认客户端下拉可正常展示
8. 在客户端管理页点击“认证测试”
9. 确认认证成功后页面显示“已认证”或等价状态

## 4. 风险点

- 当前仓库后端入口与现有项目领域不一致，需先修正基础骨架
- 当前数据库迁移配置不完整，本轮优先保证模型与启动闭合
- 前端其余页面仍存在未实现接口，本轮只保证客户端管理链路可用
