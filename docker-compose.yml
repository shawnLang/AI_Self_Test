services:
  ai-self-test:
    build: .
    container_name: ai_self_test
    ports:
      - "3000:3000"
      - "3001:3001"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - ./server/db:/app/server/db
