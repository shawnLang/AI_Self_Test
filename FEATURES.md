# 项目功能列表

本文档基于当前代码梳理，描述 `AI_Self_Test` 已实现的主要功能、后端接口、数据模型和当前边界。

## 1. 系统概览

本项目是一个“人工智能自检”Web 应用：前端使用 Vite、React 和 Tailwind CSS，后端使用 Express，数据存储使用 SQLite（`better-sqlite3`）。系统围绕“项目接入、任务配置、三方数据查询、AI 多模态识别、结果复核”展开。

主要数据流：

1. 在“项目管理”中维护外部客户端 API 地址与账号信息。
2. 在“任务管理”中创建任务，保存筛选条件与执行方式。
3. 从外部客户端 API 查询影像/媒体数据，选择数据后下发识别任务。
4. 后端调用配置的多模态模型识别媒体内容，并生成复核记录。
5. 在“结果复核”中对比原系统结果与 AI 结果，执行确认或删除。

## 2. 前端页面功能

### 2.1 全局导航与主题

- 左侧栏提供“总览、项目管理、任务管理、结果复核、模型管理、模型测试”入口。
- 支持浅色/深色主题切换，通过给 `document.documentElement` 添加或移除 `dark` 类实现。
- “设置”按钮目前仅展示在侧边栏，当前代码中未绑定具体设置页面或操作。

### 2.2 总览

- 展示活跃任务数、今日处理数、待复核数和异常任务数。
- 展示最近完成或失败的任务活动，包括任务 ID、任务名、状态、处理进度和完成时间。
- 数据来源为 `GET /api/dashboard/stats`。

### 2.3 项目管理

- 支持查看项目列表，字段包括项目名称、API 地址、账户和状态。
- 支持新增、编辑、删除项目。
- 项目表单字段包括：项目名称、API 地址、账户、密码、状态（活跃/停用）。
- 删除项目时会删除该项目关联的任务记录；当前删除项目接口未显式清理这些任务对应的复核记录。

### 2.4 任务创建

- 支持选择目标项目、执行间隔和执行方式。
- 执行间隔选项包括：每 15 分钟、每 30 分钟、每小时、每 6 小时、每天 00:00。
- 执行方式包括自动执行和手动执行，任务名称会按“项目名-自动/手动任务-时间戳”自动生成。
- 支持保存筛选条件：识别分类、关键词、物种名称、文件格式、识别类型、上传类型、开始时间、结束时间、分页大小和页码。
- 右侧展示任务配置概览，提交后写入本地 `tasks` 表。

### 2.5 任务管理

- 支持任务列表展示，并每 2 秒轮询刷新。
- 展示任务名称、项目名称、执行间隔、执行方式、状态、进度和处理数量。
- 支持删除任务；删除任务时会同步删除关联复核记录。
- 支持“查询数据并执行”，进入三方数据查询页面。
- 支持“立即执行”，后端按任务保存的筛选条件查询三方数据并开始识别。
- 支持暂停/停止运行中任务，当前接口会将 `active` 设为 `0`，并把运行中任务状态改为 `paused`。

### 2.6 三方数据查询与下发

- 页面进入后会加载任务保存的筛选条件。
- 支持按识别分类、关键词、物种名称、文件格式、识别类型、上传类型和日期范围查询外部客户端数据。
- 查询结果以卡片网格展示，支持图片、视频封面、分类标签、物种名、设备名和文件时间。
- 支持单选、多选、全选当前页。
- 支持媒体预览：图片以大图查看，视频使用播放器查看。
- 支持将已选数据下发给后端执行识别任务。

### 2.7 结果复核

- 支持按已完成任务选择复核范围。
- 默认拉取 `pending` 状态复核记录。
- 支持“全部、一致、不一致”筛选；一致性判断会对原系统结果和 AI 结果做大小写、空白和分隔符归一化后比较。
- 支持列表、网格、画廊三种视图。
- 画廊视图支持上一项/下一项切换，并绑定左右方向键。
- 支持图片和视频预览。
- 支持单条确认、单条删除、批量确认和批量删除。
- 当前“确认并更新”在后端表现为将复核记录状态更新为 `confirmed`，未看到回写第三方系统的接口调用。

### 2.8 多模态模型管理

- 支持查看、新增、编辑、删除多模态模型配置。
- 模型配置字段包括：模型名称、模型接口地址、API Key/密码、状态、已检索模型列表、最近检索时间。
- 输入接口地址和 API Key 后会自动尝试检索模型列表，也可手动点击“自动检索模型”。
- 模型列表检索兼容 `/v1/models` 和 `/models` 等常见路径。
- 后端启动时会根据 `OMLX_API_URL`、`OMLX_API_KEY`、`OMLX_MODEL` 自动注册默认模型配置。

### 2.9 多模态模型测试

- 支持选择启用状态的多模态模型进行测试。
- 支持输入文本并上传附件，附件类型包括图片、视频、音频和文档。
- 文本类文件会读取为文本内容；图片会作为 `image_url` 发送；音频会从 Data URL 提取 base64 后发送；其他附件会以文本描述形式进入请求。
- 支持查看已发送附件、预览图片/视频/音频/PDF、打开不支持内嵌预览的文档。
- 支持复制模型回复、清空当前会话、刷新模型列表。
- 当前发送逻辑每次只向后端提交本轮用户消息，不会把页面中的历史对话作为上下文一并发送。

## 3. 后端 API 功能

| 接口 | 功能 |
| --- | --- |
| `GET /api/clients` | 获取项目列表 |
| `POST /api/clients` | 新增项目 |
| `PUT /api/clients/:id` | 编辑项目 |
| `DELETE /api/clients/:id` | 删除项目及其任务 |
| `GET /api/tasks` | 获取任务列表与项目名 |
| `GET /api/tasks/:id` | 获取单个任务及其筛选条件 |
| `POST /api/tasks` | 创建任务 |
| `DELETE /api/tasks/:id` | 删除任务及其复核记录 |
| `PUT /api/tasks/:id/status` | 更新任务启停状态 |
| `POST /api/tasks/:id/query-data` | 按筛选条件查询三方客户端数据 |
| `POST /api/tasks/:id/run-now` | 按保存条件立即执行任务 |
| `POST /api/tasks/:id/execute` | 执行已选数据识别任务 |
| `GET /api/reviews` | 获取待复核记录，可按任务筛选 |
| `GET /api/reviews/completed-tasks` | 获取已完成且可复核的任务 |
| `DELETE /api/reviews/:id` | 删除单条复核记录 |
| `POST /api/reviews/confirm` | 批量确认复核记录 |
| `POST /api/reviews/delete` | 批量删除复核记录 |
| `GET /api/dashboard/stats` | 获取总览统计 |
| `GET /api/multimodal-models` | 获取多模态模型配置 |
| `POST /api/multimodal-models/detect` | 自动检索远端模型列表 |
| `POST /api/multimodal-models` | 新增模型配置 |
| `PUT /api/multimodal-models/:id` | 编辑模型配置 |
| `DELETE /api/multimodal-models/:id` | 删除模型配置 |
| `POST /api/multimodal-models/:id/chat` | 调用指定多模态模型进行测试 |

## 4. 数据模型

| 表 | 主要用途 |
| --- | --- |
| `clients` | 存储外部项目/客户端信息，包括 API 地址、账号、密码、状态、访问令牌、刷新令牌和过期时间。 |
| `tasks` | 存储任务配置，包括项目 ID、执行间隔、筛选条件 JSON、执行模式、状态、总数、已处理数、开始/结束时间和错误信息。 |
| `reviews` | 存储 AI 识别后的复核记录，包括媒体地址、原系统结果、AI 结果、任务信息、文件 ID、媒体类型和创建时间。 |
| `multimodal_models` | 存储多模态模型配置，包括模型名、接口地址、API Key、状态、检测到的模型列表和更新时间。 |

## 5. 外部集成

- 客户端数据接口：后端会访问外部项目的 `/auth/login`、`/auth/refresh` 和 `/openApi/icFile/findFilePage`。
- 鉴权处理：支持使用保存的 `access_token`，过期时尝试刷新，刷新失败则重新登录；查询失败时会尝试 Bearer token 兼容模式。
- 媒体地址解析：根据外部 API 地址拼接 `/weed/{fileUrl}`，并通过文件扩展名推断图片或视频。
- AI 识别接口：默认使用 OpenAI Chat Completions 风格的请求体，发送 `model`、`messages`、`temperature` 和 `max_tokens`。
- AI 识别结果归一化：优先解析 JSON 中的 `species`、`result` 或 `name` 字段，失败时回退到首行文本，并对“无/none/unknown/人类”等结果做标准化。

## 6. 当前功能边界

- 自动执行模式和执行间隔已保存并展示，但当前代码未看到后台定时调度循环；实际执行入口主要是“立即执行”和“查询数据并执行”。
- 后端 API 当前未实现应用级登录鉴权，默认认为访问者可信。
- 项目和模型配置会保存账号、密码或 API Key，请将 SQLite 数据库视为敏感本地状态。
- `server/db/` 中的 SQLite 文件是运行数据，不应作为通用源代码资产。
- “设置”入口目前是 UI 占位。
